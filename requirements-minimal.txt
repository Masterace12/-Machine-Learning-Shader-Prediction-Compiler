# Steam Deck Minimal Requirements - Ultra-lightweight for limited resources
# Minimal dependencies optimized for Steam Deck's memory and performance constraints

# Core numerical computing - Essential only
numpy>=1.21.0,<1.25.0
psutil>=5.8.0,<6.0.0

# Essential system libraries
PyYAML>=6.0,<7.0
requests>=2.28.0,<3.0.0

# Lightweight ML - Only if memory allows (>4GB available)
scikit-learn>=1.1.0,<1.4.0
joblib>=1.1.0,<1.4.0

# Steam Deck specific hardware monitoring (Linux only)
pyudev>=0.23.0; platform_system=="Linux"

# Configuration
configparser>=5.2.0

# Optional networking (only install if P2P features needed and bandwidth available)
# aiohttp>=3.8.0,<4.0.0  # Commented out to save ~50MB RAM
# cryptography>=37.0.0,<42.0.0  # Commented out to save ~30MB RAM

# Note: Install full requirements.txt if Steam Deck has >8GB RAM available
# Use: pip install -r requirements.txt instead of requirements-minimal.txt