# Steam Deck Optimized Requirements - ML Shader Prediction Compiler
# Minimal dependencies optimized for Steam Deck's memory and performance constraints

# Core numerical computing - Steam Deck compatible versions
numpy>=1.21.0,<1.25.0
psutil>=5.8.0,<6.0.0

# Lightweight ML - Reduced memory footprint for Steam Deck
scikit-learn>=1.1.0,<1.4.0
joblib>=1.1.0,<1.4.0

# Essential system libraries
PyYAML>=6.0,<7.0

# Optional enhanced features (install only if memory allows)
# pandas>=1.4.0,<2.1.0  # Commented out to save ~100MB RAM
# scipy>=1.8.0,<1.12.0  # Commented out to save ~50MB RAM

# Steam Deck specific hardware monitoring (Linux only)
pyudev>=0.23.0; platform_system=="Linux"

# Configuration and logging
configparser>=5.2.0

# Optional networking (only install if P2P features needed)
# aiohttp>=3.8.0,<4.0.0  # Commented out to save memory
# cryptography>=37.0.0,<42.0.0  # Commented out to save memory