# Core ML and Data Science
numpy>=1.21.0,<2.0.0
scikit-learn>=1.1.0,<1.5.0
pandas>=1.4.0,<2.2.0
scipy>=1.8.0,<1.12.0
joblib>=1.1.0,<1.4.0

# Networking and P2P
aiohttp>=3.8.0,<4.0.0
cryptography>=3.4.8,<42.0.0
pycryptodome>=3.15.0,<4.0.0

# System Integration (Steam Deck compatible)
psutil>=5.8.0,<6.0.0
py-cpuinfo>=8.0.0,<10.0.0

# Steam Deck Hardware (Linux only)
pyudev>=0.23.2,<1.0.0; platform_system=="Linux"
dbus-python>=1.2.18,<2.0.0; platform_system=="Linux"

# Security and Validation
requests>=2.28.0,<3.0.0
# Note: hashlib is part of standard library, no need to specify version
# Note: pyspirv-cross removed as it's not available on PyPI and not needed for basic functionality

# Steam Deck GPU support (optional, fallback to CPU inference)
# These are optional for better GPU acceleration on Steam Deck
torch>=1.12.0,<2.1.0; extra == "gpu"

# Configuration and Logging
PyYAML>=6.0
toml>=0.10.2
configparser>=5.2.0

# GUI and Visualization (optional)
matplotlib>=3.5.0
seaborn>=0.11.0
plotly>=5.10.0

# Testing
pytest>=7.0.0
pytest-cov>=3.0.0
pytest-asyncio>=0.19.0

# Development
black>=22.0.0
flake8>=4.0.0
mypy>=0.950
bandit>=1.7.0